import type { Icon as LucideIcon } from "lucide-svelte";
import {
	Check,
	Copy,
	Moon
} from "lucide-svelte";

export type Icon = LucideIcon;

export const Icons = {
	check: Check,
	copy: Copy,
	moon: Moon,
};
